package com.hommy.service;

import com.hommy.service.interfaces.ITopic;
import com.hommy.service.topics.TemperatureTopic;

public class ObserverPatternTest {

	public static void main(String[] args) {
		// create subject
		ITopic tempopic = new TemperatureTopic();

		// create observers
		IObserver obj1 = new TopicSubscriber("Obj1");
		IObserver obj2 = new TopicSubscriber("Obj2");
		IObserver obj3 = new TopicSubscriber("Obj3");

		// register observers to the subject
		topic.register(obj1);
		topic.register(obj2);
		topic.register(obj3);

		// attach observer to subject
		obj1.setSubject(topic);
		obj2.setSubject(topic);
		obj3.setSubject(topic);

		// check if any update is available
		obj1.update();

		// now send message to subject
		topic.postMessage("New Message");
	}
}
